name: .NET Core

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: younos1986/Core.EventStore
    - name: Setup .NET Core app
      with:
        dotnet-version: 3.1.101
    - name: Build with dotnet
      run: dotnet build ./Core.EventStore.sln --configuration Release
